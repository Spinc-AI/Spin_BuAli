# Copy to ".env" and adjust. Every value is optional; the defaults in
# config.py apply when it is absent.

# HOST=0.0.0.0
# PORT=8002

# Score against a different concept vocabulary without changing code.
# Note that this changes results, so the terms_sha in every output changes too.
# CLINICAL_TERMS_PATH=clinical_terms.json
